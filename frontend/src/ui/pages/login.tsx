import { LoginForm } from "../components/login-form";

const Login = () => {
    return (
        <main className="bg-gray-900 w-full h-screen flex justify-center items-center">
            <LoginForm className="w-[30%] h-[60%] "></LoginForm>
        </main>
    );
};

export default Login;
